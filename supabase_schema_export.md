| CREATE TABLE appointments (status text, time text, type text, created_at timestamp with time zone, date date, pet_id uuid, doctor_id uuid, user_id uuid, id uuid);                                                              |
| CREATE TABLE availability_slots (doctor_id uuid, id uuid, start_time text, end_time text, day text);                                                                                                                            |
| CREATE TABLE chat_participants (user_id uuid, id integer, joined_at timestamp with time zone, chat_id integer);                                                                                                                 |
| CREATE TABLE chats (id integer, created_at timestamp with time zone, updated_at timestamp with time zone, name text, type text);                                                                                                |
| CREATE TABLE comment_likes (user_id uuid, comment_id bigint, created_at timestamp with time zone);                                                                                                                              |
| CREATE TABLE doctors (clinic text, verified boolean, user_id uuid, next_available text, specialty text, rating numeric, created_at timestamp with time zone, id uuid, reviews_count integer, available boolean);                |
| CREATE TABLE friendships (status text, receiver_id uuid, created_at timestamp with time zone, requester_id uuid, id bigint);                                                                                                    |
| CREATE TABLE messages (edited_at timestamp with time zone, chat_id integer, content text, id integer, created_at timestamp with time zone, sender_id uuid);                                                                     |
| CREATE TABLE notifications (is_read boolean, actor_id uuid, user_id uuid, id bigint, type text, title text, body text, related_id bigint, created_at timestamp with time zone);                                                 |
| CREATE TABLE pet_health_events (notes text, date date, pet_id uuid, created_at timestamp with time zone, id bigint, type text, title text);                                                                                     |
| CREATE TABLE pets (health text, next_checkup date, created_at timestamp with time zone, id uuid, owner_id uuid, name text, type text, breed text, age text, emoji text, image_url text, gender text, weight text, height text); |
| CREATE TABLE post_comments (id bigint, post_id bigint, created_at timestamp with time zone, likes_count bigint, content text, user_id uuid);                                                                                    |
| CREATE TABLE post_likes (post_id bigint, created_at timestamp with time zone, user_id uuid);                                                                                                                                    |
| CREATE TABLE posts (location text, id bigint, content text, image_url text, tags ARRAY, author_name text, author_image text, comments_count integer, likes_count integer, user_id uuid, created_at timestamp with time zone);   |
| CREATE TABLE reviews (id bigint, doctor_id uuid, user_id uuid, rating numeric, created_at timestamp with time zone, comment text);                                                                                              |
| CREATE TABLE users (bio text, id uuid, created_at timestamp with time zone, email text, name text, phone text, role text, profile_image text, specialty text, clinic text, username text);                                      |