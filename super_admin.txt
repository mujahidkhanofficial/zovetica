-- Step 1: Create auth user (replace with your credentials)
-- Note: This creates a user in auth.users - Supabase handles password hashing
INSERT INTO auth.users (
  instance_id,
  id,
  aud,
  role,
  email,
  encrypted_password,
  email_confirmed_at,
  created_at,
  updated_at,
  confirmation_token,
  recovery_token
)
VALUES (
  '00000000-0000-0000-0000-000000000000',
  gen_random_uuid(),
  'authenticated',
  'authenticated',
  'admin@zovetica.com',  -- Change this email
  crypt('zovetica@786', gen_salt('bf')),  -- Change this password
  NOW(),
  NOW(),
  NOW(),
  '',
  ''
);

-- Step 2: Create matching public.users record with super_admin role
INSERT INTO public.users (id, email, name, role)
SELECT id, email, 'Super Admin', 'super_admin'
FROM auth.users 
WHERE email = 'admin@zovetica.com';